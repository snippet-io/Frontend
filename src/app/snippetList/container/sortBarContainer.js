import SortBar from "../components/sortBar/sortBar";
import languageList from "lib/languages";

const SortBarContainer = () => {
  return <SortBar languages={languageList}></SortBar>;
};

export default SortBarContainer;
